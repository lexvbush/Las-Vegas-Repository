# Tagged files staged for upload — local only, never committed
daily/
# exiftool backups
*_original
__pycache__/
*.pyc

# macOS
.DS_Store
# working backups taken before a bulk edit
*.bak
*.bak[0-9]
# regenerated by `lvtag audit` on every run
manifest/audit_report.csv

# manifest/push_log.csv is NOT ignored on purpose -- it is the old-value trail
# that makes a bad push revertible, so it belongs in git.

# Airtable personal access token -- a credential, never committed
.env
